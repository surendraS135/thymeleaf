kind: pipeline
type: docker
name: default

steps:
  - name: deploy-app
    image: docker:20.10.7  # Regular Docker image
    commands:
      - docker pull sowjanya135/drone-mvn:latest  # Pull the image
      - docker run -itd -p 8080:8086 --name drone-docker sowjanya135/drone-mvn:latest  # Run the container
